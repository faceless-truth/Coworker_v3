import ComingSoon from '../components/ComingSoon'
export default function Memory() {
  return <ComingSoon title="Memory" description="Search and browse client interactions, learned lessons, and indexed documents from SharePoint." endpoint="GET /api/v1/memory" />
}
